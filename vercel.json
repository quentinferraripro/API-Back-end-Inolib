{
  "buildCommand": "",
  "devCommand": "vite --port $PORT",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "./api",
  "rewrites": [{ "source": "/", "destination": "/api" }]
}
