extend type Query {
  documents: [Document!]!
}

type Document {
  id: Cuid!
  title: String!
  content: String!
  createdAt: DateTime!
}

extend type Mutation {
  newDocumentRequest(title: String!, content: String!): Document
}
